services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - app_data:/app/data
    environment:
      - APP_ENV=production

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  app_data:
